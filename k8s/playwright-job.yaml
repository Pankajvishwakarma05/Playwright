apiVersion: batch/v1
kind: Job
metadata:
  name: playwright-test-job
spec:
  template:
    spec:
      containers:
      - name: playwright
        image: playwright-tests:latest
        imagePullPolicy: Never
      restartPolicy: Never
  backoffLimit: 1
